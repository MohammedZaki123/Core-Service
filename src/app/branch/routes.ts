import { branchController } from "./controller/branch.controller";

import {Router} from "express";

export const branchRouter = Router({mergeParams : true});


branchRouter.get('',);

branchRouter.post('', );

branchRouter.patch('/:branchId', );

branchRouter.patch('/:branchId/status');

